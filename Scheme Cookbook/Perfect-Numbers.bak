#lang simply-scheme

; Find perfect numbers

;(define (sum-of-factors n a)
  ;(if (> a (sqrt n)) 0
      ;(+ 1 (factor n a) (sum-of-factors n (+ 1 a)))))

(define (factor n a)
  (if (equal? (remainder n a) 0) a
      0))
